<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= pageTitle %></title>
    <style>
        body { font-family: Arial, sans-serif; background: #f4f4f4; padding: 20px; }
        .container { max-width: 1200px; margin: auto; background: white; padding: 20px; border-radius: 8px; box-shadow: 0 0 10px rgba(0,0,0,0.1); }
        .nav-bar { margin-bottom: 20px; padding-bottom: 10px; border-bottom: 2px solid #eee; }
        .nav-bar a { text-decoration: none; color: #333; margin-right: 15px; font-weight: bold; }
        .nav-bar a:hover { color: #007bff; }

        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { padding: 12px; border-bottom: 1px solid #ddd; text-align: left; }
        th { background-color: #17a2b8; color: white; }
        tr:hover { background-color: #f1f1f1; }
        
        .score-box { font-weight: bold; padding: 5px; background: #eee; border-radius: 4px; }
        .appeal-status { font-size: 0.9em; color: #dc3545; font-weight: bold; }

        /* Buttons */
        .btn-edit { background-color: #ffc107; color: black; padding: 5px 10px; border-radius: 4px; text-decoration: none; font-size: 0.8em; font-weight: bold; margin-right: 5px; }
        .btn-delete { background-color: #dc3545; color: white; border: none; padding: 5px 10px; border-radius: 4px; cursor: pointer; font-size: 0.8em; }
        .btn-appeal { background-color: #6f42c1; color: white; padding: 5px 10px; border-radius: 4px; text-decoration: none; font-size: 0.8em; }
    </style>
</head>
<body>
    <div class="container">
        <div class="nav-bar">
            <a href="/">Employees</a>
            <a href="/departments">Departments</a>
            <a href="/jobs">Jobs</a>
            <a href="/assignments">Assignments</a>
            <a href="/training">Training</a>
            <a href="/performance" style="color: #007bff;">Performance</a>
        </div>

        <h1>Performance Appraisals</h1>

        <div style="text-align: right; margin-bottom: 15px;">
            <a href="/add-cycle" style="background-color: #6c757d; color: white; padding: 10px 15px; text-decoration: none; border-radius: 5px; font-weight: bold; margin-right: 10px;">+ New Cycle</a>
            <a href="/add-appraisal" style="background-color: #17a2b8; color: white; padding: 10px 15px; text-decoration: none; border-radius: 5px; font-weight: bold;">+ New Appraisal</a>
        </div>
        
        <table>
            <thead>
                <tr>
                    <th>Cycle</th>
                    <th>Employee</th>
                    <th>Date</th>
                    <th>Score</th>
                    <th>Comments</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% if(appraisals.length === 0) { %>
                    <tr><td colspan="6">No performance records found.</td></tr>
                <% } else { %>
                    <% appraisals.forEach(appr => { %>
                    <tr>
                        <td>
                            <%= appr.PerformanceCycle ? appr.PerformanceCycle.Cycle_Name : 'N/A' %>
                            <% if (appr.PerformanceCycle) { %>
                                <br>
                                <a href="/edit-cycle/<%= appr.PerformanceCycle.Cycle_ID %>" style="font-size: 0.7em; color: #007bff;">Edit Cycle</a>
                            <% } %>
                        </td>
                        <td>
                            <% if (appr.JobAssignment && appr.JobAssignment.Employee) { %>
                                <%= appr.JobAssignment.Employee.First_Name %> <%= appr.JobAssignment.Employee.Last_Name %>
                            <% } else { %>
                                Unknown Employee
                            <% } %>
                        </td>
                        <td><%= appr.Appraisal_Date %></td>
                        <td><span class="score-box"><%= appr.Overall_Score %></span></td>
                        <td><%= appr.Manager_Comments %></td>
                        <td style="min-width: 200px;">
                            <a href="/edit-appraisal/<%= appr.Appraisal_ID %>" class="btn-edit">Edit</a>

                            <form action="/delete-appraisal" method="POST" style="display:inline;" onsubmit="return confirm('Delete this record?');">
                                <input type="hidden" name="Appraisal_ID" value="<%= appr.Appraisal_ID %>">
                                <button type="submit" class="btn-delete">Del</button>
                            </form>

                            <% if (appr.Appeal) { %>
                                <div class="appeal-status">Appeal: <%= appr.Appeal.Approval_Status %></div>
                            <% } else { %>
                                <a href="/add-appeal/<%= appr.Appraisal_ID %>" class="btn-appeal" style="margin-left: 5px;">Appeal</a>
                            <% } %>
                        </td>
                    </tr>
                    <% }) %>
                <% } %>
            </tbody>
        </table>
    </div>
</body>
</html>